[package]
name = "node-bbs-signatures"
description = "A node wrapper for bbs-signatures"
version = "0.1.0"
build = "build.rs"
edition = "2018"

[lib]
name = "node_bbs_signatures"
crate-type = ["cdylib"]

[build-dependencies]
neon-build = "0.4.0"

[dependencies]
bbs = { git = "https://github.com/mikelodder7/ursa", branch = "deterministic_bbs", rev = "54778517" }
neon = "0.4.0"